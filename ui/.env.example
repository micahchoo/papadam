# papadam UI — local development environment
# Copy to .env.local (gitignored by Vite) and fill in values.
# In production these are not used — the SPA fetches /config.json from Django instead.

VITE_API_URL=http://localhost:8000
VITE_CRDT_URL=ws://localhost:1234
