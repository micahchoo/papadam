# Settings package — each module exports one Configuration class.
# Use DJANGO_SETTINGS_MODULE=papadapi.config.<env> + DJANGO_CONFIGURATION=<Class>.
# Do not add imports here — they would force all config modules to load at once.
