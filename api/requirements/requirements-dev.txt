-r requirements.txt
-r requirements-test.txt

# Linting & formatting
ruff==0.6.9                # replaces flake8 + isort + pyupgrade
mypy==1.11.2               # type checking
django-stubs==5.1.0        # Django type stubs for mypy
djangorestframework-stubs==3.15.1

# Architecture enforcement
import-linter==2.1         # dependency contract enforcement (architecture as code)

# Security
bandit==1.7.10             # security linting

# Dev server
django-silk                # profiling (dev only)
